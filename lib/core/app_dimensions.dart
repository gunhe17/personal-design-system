class AppDimensions {
  // --- 컴포넌트 높이 (Component Heights) ---
  static const double componentHeight = 40.0;

  // --- 요소 간 간격 (Inter-element Spacing) ---
  static const double gapDescription = 8.0;

  // --- 폼 레이아웃 (Form Layouts) ---
  static const double formMaxWidth = 600.0;

  // --- Border Radius ---
  static const double radiusSm = 2.0;
  static const double radiusMd = 6.0;
  static const double radiusLg = 8.0;
  static const double radiusXl = 12.0;
  static const double radiusFull = 9999.0;

  // --- 레이아웃 ---
  static const double contentMaxWidth = 1200.0;
}
